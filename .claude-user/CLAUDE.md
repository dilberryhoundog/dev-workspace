## Syntax Helpers
### Markdown files
`%% comment %%` - These are comment instructions useful for ai agents when editing .md files. Remove the comment and/or tags after editing. 

### mermaid diagrams
`||--|{` - not null foreign key (required reference)  
`||--o{` - nullable foreign key (optional reference)  
